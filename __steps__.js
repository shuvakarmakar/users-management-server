/**
 * 1. Create a post api on the server side
 * 2. client side send data via post
 * 3. send fetch method  inside the fetch options (seconf parameter)
 * 4. Option will have three things: methods
 * 5. option will have headers: 'content-type' : 'application/json'
 * 6. don't foregt to send data  by JSON.Stringify in the body:
 * 7. On the server side do not forget to use : express.json() middleware
 */